<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shivam.shivamsood_comp304_assignment4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CLogin" />
        <activity android:name=".CRegister" />
        <activity android:name=".Customer" />
        <activity android:name=".CustomerMenu" />
        <activity android:name=".OrderInformation" />
        <activity android:name=".Representative" />
        <activity android:name=".RLogin" />
        <activity android:name=".RRegister" />
        <activity android:name=".ShoppingScreen" />
        <activity android:name=".ViewDetails" />
        <activity android:name=".RepresentativeMenu"></activity>
    </application>

</manifest>